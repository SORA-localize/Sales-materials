# ローカライズ校正ガイド

このサイトはAI翻訳 vs 人間校正の差をインタラクティブに見せるための営業デモです。

---

## モードの概要

| モード | 表示内容 |
|--------|---------|
| **Original** | 英語のまま（元のサイト） |
| **AI** | AI翻訳を適用した日本語 |
| **Proofread** | 人間が校正した日本語。AIと異なる箇所は黄色ハイライト＋ツールチップで理由を表示 |

初期状態では **AI = Proofread**（差分なし・ハイライトなし）。  
あなたが `proofread` を書き換えるたびに、その箇所が自動でハイライト表示されます。

---

## 校正の手順

### 1. 翻訳ファイルを開く

```
lib/translations.js
```

### 2. 校正したいキーを見つけて `proofread` を書き換える

各キーの構造：

```js
hero_title: {
  original: "Gadgets you'll love. Prices you'll trust.",  // 英語（触らない）
  ai:       "あなたが愛するガジェット。信頼できる価格。",           // AI翻訳（触らない）
  proofread: "あなたが愛するガジェット。信頼できる価格。",          // ← ここだけ書き換える
},
```

**校正後（例）：**

```js
hero_title: {
  original: "Gadgets you'll love. Prices you'll trust.",
  ai:       "あなたが愛するガジェット。信頼できる価格。",
  proofread: "心躍るガジェット、納得の価格。",          // ai と変えた → ハイライト表示される
  note: "直訳調でコピーとして弱い。日本語ECらしいリズム感に修正",  // ← ホバー時のツールチップ（省略可）
},
```

**ルール：**
- `original` と `ai` は変更しない
- `proofread` を `ai` と違う値にすると → 自動でハイライト表示
- `note` を追加すると → ホバー時にツールチップで理由を表示（なくてもOK）

### 3. 保存 → ブラウザ自動更新

ファイルを保存するとホットリロードで即座に反映されます。

**確認手順：**
1. ブラウザのヘッダーで **Proofread** をクリック
2. 修正した箇所が黄色ハイライトされているか確認
3. ハイライト部分にホバーして `note` の内容が表示されるか確認
4. Proofreadボタンの右上バッジの数字が増えているか確認

---

## 変数プレースホルダーについて

動的な数値が入るキーは `{変数名}` 形式です。校正時もこの形式を維持してください。

```js
section_showing_n_of_m: {
  original: 'Showing {shown} of {total} products',
  ai: '{total}件中{shown}件を表示中',
  proofread: '{total}件中{shown}件を表示',  // {shown} と {total} はそのまま残す
},
```

---

## ファイル構成（参考）

```
lib/
  translations.js      ← 校正作業はここだけ
  LanguageContext.jsx   ← グローバルなモード管理（変更不要）
components/
  T.jsx                ← 翻訳コンポーネント（変更不要）
  Navbar.jsx           ← モード切替ボタン
```
